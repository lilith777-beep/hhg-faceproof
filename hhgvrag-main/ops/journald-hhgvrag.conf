# Installed by ops/install.sh to /etc/systemd/journald.conf.d/hhgvrag.conf
# Caps the persistent journal so hhgvrag (and everything else on this shared box) cannot fill the
# disk. This is a GLOBAL journald setting (drop-in), not per-unit. 2G is ample for days of logs.
[Journal]
SystemMaxUse=2G
