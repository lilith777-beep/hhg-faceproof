# hhgvrag — Voice-Enabled RAG
# Split by where it runs. Local dev (Python 3.11) only needs the "core" + "dev" blocks;
# the heavy GPU libs run on Modal.

# --- core (local dev + Modal) ---
pydantic>=2.5
numpy>=1.24
httpx>=0.27               # Sarvam STT API + general HTTP
python-dotenv>=1.0
qdrant-client>=1.9        # ':memory:' locally, server on Modal
datasets>=2.16            # HuggingFace: load ai4bharat/MSMARCO-XI
fastapi>=0.110
uvicorn[standard]>=0.27

# --- embeddings + generation (Modal GPU; heavy — do NOT install on the 2GB laptop) ---
# FlagEmbedding>=1.2      # BGE-M3 (dense + sparse + ColBERT)
# torch>=2.2              # CUDA build, provided by the Modal image
# transformers>=4.44
# accelerate>=0.30
# vllm>=0.5               # optional: fast local serving of the 3B generator

# --- orchestration / deploy ---
# modal>=0.64             # `pip install modal` when we wire the GPU app

# --- dev ---
pytest>=8.0
