[pytest]
# Playwright e2e config for the hhgvrag Opus-UI acceptance suite.
# Raw playwright.sync_api (no pytest-playwright); browser/context/page lifecycle,
# fixtures and backend stubs live in conftest.py.
# Run:  .venv/Scripts/python -m pytest tests/e2e -v
addopts = -ra
testpaths = .
