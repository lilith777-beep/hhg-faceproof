# Synthetic acceptance fixtures

These three entirely fictional portraits were generated with OpenAI's built-in image-generation
tool on 2026-09-01 solely for local pipeline testing:

- `query.png`: fictional reference subject;
- `same-person.png`: identity-preserving variation of the fictional reference;
- `different-person.png`: a different fictional subject used as a negative.

They contain no known real identity and must never be presented as a genuine social-media search.
The acceptance command labels its source `synthetic-placeholder-source`. The final submission must
still use an explicitly consenting person and a genuinely discovered public post.
